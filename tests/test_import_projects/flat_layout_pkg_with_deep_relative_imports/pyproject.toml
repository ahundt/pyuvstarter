[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "anotherproject"
version = "1.0.0"
description = "Flat-layout test package for relative import fixing"

[tool.uv]
package = true
dev-dependencies = [
    "ruff>=0.1.0",
]