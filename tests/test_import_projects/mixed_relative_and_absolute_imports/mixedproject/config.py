"""
Config module with absolute imports (should stay as-is).
"""

import json
import os

def get_config():
    """Get configuration using absolute imports."""
    return {"debug": True, "version": "1.0"}